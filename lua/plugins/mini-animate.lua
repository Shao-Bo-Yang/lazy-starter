return {
  "echasnovski/mini.animate",
  opt = {
    cursor = {
      enable = false,
    },

    -- Vertical scroll
    scroll = {
      enable = false,
    },

    -- Window resize
    resize = {
      enable = false,
    },

    -- Window open
    open = {
      enable = false,
    },

    -- Window close
    close = {
      enable = false,
    },
  },
}
